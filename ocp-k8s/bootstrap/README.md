# Bootstrapping OCP
This is just a basic setup for a new OpenShift cluster for development. It will set up and install the following Operators and get them ready for use:

- OpenShift GitOps
- OpenShift Pipelines
